version: '3.8'

services:
  angular-app:
    build: .
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
